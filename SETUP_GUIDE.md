# AI Pic Center - 设置指南

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

创建 `.env.local` 文件（在项目根目录）：

```env
# Google Gemini API 配置
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# 应用 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**重要**: 
- 从 [Google AI Studio](https://makersuite.google.com/app/apikey) 获取 Gemini API 密钥
- 将 `your_gemini_api_key_here` 替换为实际的 API 密钥
- 不要将 `.env.local` 文件提交到版本控制

### 3. 运行开发服务器

```bash
npm run dev
```

打开浏览器访问 [http://localhost:3000](http://localhost:3000)

### 4. 构建生产版本

```bash
npm run build
npm start
```

---

## 📁 项目结构说明

```
aIpiccenter/
├── app/                      # Next.js App Router 页面和路由
│   ├── api/                  # API 路由（后端接口）
│   │   ├── generate/         # 图像/视频生成 API
│   │   └── enterprise-inquiry/ # 企业询价 API
│   ├── generate/             # 图像生成页面
│   ├── pricing/              # 定价页面
│   ├── layout.tsx            # 根布局（Header + Footer）
│   ├── page.tsx              # 首页
│   └── globals.css           # 全局样式
│
├── components/               # React 组件
│   ├── home/                 # 首页组件
│   │   ├── Hero.tsx         # 首屏 Hero 区域
│   │   ├── TrustBadges.tsx  # 技术背书
│   │   └── UserCases.tsx    # 用户案例
│   ├── generate/             # 生成页面组件
│   │   ├── PromptInput.tsx  # Prompt 输入框
│   │   ├── AdvancedSettings.tsx # 高级设置
│   │   ├── GenerationProgress.tsx # 生成进度
│   │   └── ResultCard.tsx  # 结果卡片
│   ├── pricing/              # 定价页面组件
│   │   ├── PricingCard.tsx  # 定价卡片
│   │   └── EnterpriseInquiry.tsx # 企业询价表单
│   └── layout/               # 布局组件
│       ├── Header.tsx        # 导航栏
│       └── Footer.tsx        # 页脚
│
├── lib/                      # 工具库和业务逻辑
│   ├── gemini.ts            # Gemini API 集成
│   └── store.ts             # Zustand 状态管理
│
├── types/                    # TypeScript 类型定义
│   └── index.ts             # 所有类型定义
│
├── public/                   # 静态资源（图片、图标等）
│
└── 配置文件
    ├── package.json         # 项目依赖和脚本
    ├── tsconfig.json        # TypeScript 配置
    ├── tailwind.config.ts   # Tailwind CSS 配置
    ├── next.config.js       # Next.js 配置
    └── .eslintrc.json       # ESLint 配置
```

---

## 🔧 核心功能说明

### 1. 首页 (Homepage)

- **Hero 区域**: 全屏动态展示，核心 CTA 按钮
- **功能导航**: 图像生成、视频生成、定价方案
- **技术背书**: "Powered by Gemini AI" 标识
- **用户案例**: 展示最新高质量作品

### 2. AI 图像生成页

- **Prompt 输入**: 支持多行输入，AI 增强提示功能
- **高级设置**: 可折叠面板，包含尺寸、负面提示、数量、模型选择
- **生成流程**: 无刷新生成，实时进度反馈
- **结果管理**: 下载、复制 Prompt、生成变体

### 3. 定价页面

- **4 个方案**: 基础版、专业版、高级版、企业版
- **信用点机制**: 1 Credit = 1 图像，10 Credits = 1 视频
- **企业询价**: 单独的表单，获取企业需求

---

## 🔌 API 集成说明

### Gemini API 集成

当前实现为**占位符**，需要替换为实际的 Gemini API 调用：

1. **图像生成**: `lib/gemini.ts` 中的 `generateImage()` 函数
2. **视频生成**: `lib/gemini.ts` 中的 `generateVideo()` 函数
3. **提示增强**: `lib/gemini.ts` 中的 `enhancePrompt()` 函数

### API 路由

- `POST /api/generate/image` - 图像生成
- `POST /api/generate/video` - 视频生成
- `POST /api/enhance-prompt` - 提示增强
- `POST /api/enterprise-inquiry` - 企业询价

---

## 🎨 样式系统

### Tailwind CSS

项目使用 Tailwind CSS 进行样式管理：

- **颜色系统**: 主色调为蓝色（primary），符合 Google 品牌
- **组件类**: 预定义的按钮、卡片、输入框样式
- **响应式**: 使用 Tailwind 的响应式断点

### 自定义样式

在 `app/globals.css` 中定义了：
- 按钮变体（primary, secondary, ghost）
- 卡片样式
- 输入框样式
- 动画效果

---

## 📱 响应式设计

项目完全响应式，支持：
- **桌面端**: 1920px+
- **平板**: 768px - 1024px
- **移动端**: < 768px

所有组件都使用 Tailwind 的响应式类（`sm:`, `md:`, `lg:` 等）

---

## ♿ 无障碍功能

- ✅ ARIA 标签完整
- ✅ 键盘导航支持
- ✅ 屏幕阅读器友好
- ✅ 语义化 HTML
- ✅ 颜色对比度符合 WCAG 标准

---

## 🚀 性能优化

### Core Web Vitals

- ✅ **LCP (Largest Contentful Paint)**: 优化首屏加载
- ✅ **FID (First Input Delay)**: 优化交互延迟
- ✅ **CLS (Cumulative Layout Shift)**: 防止布局偏移

### 优化措施

- Next.js Image 组件自动优化
- 代码分割和懒加载
- 静态资源优化
- 压缩和缓存

---

## 🔒 安全考虑

- ✅ API 密钥仅服务器端使用
- ✅ 输入验证和清理
- ✅ 错误处理不暴露敏感信息
- ✅ HTTPS 强制（生产环境）

---

## 🐛 常见问题

### 1. API 密钥错误

**问题**: `Gemini API key not configured`

**解决**: 确保 `.env.local` 文件存在且包含正确的 `NEXT_PUBLIC_GEMINI_API_KEY`

### 2. 图片加载失败

**问题**: 生成的图片无法显示

**解决**: 
- 检查图片 URL 是否有效
- 确保 `next.config.js` 中的图片域名配置正确
- 检查 CORS 设置

### 3. 样式不生效

**问题**: Tailwind CSS 样式未应用

**解决**: 
- 确保 `tailwind.config.ts` 中的 `content` 路径正确
- 重启开发服务器
- 清除 `.next` 缓存

---

## 📝 开发建议

### 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 组件化设计，保持单一职责
- 添加必要的注释

### 测试

建议添加：
- 单元测试（Jest）
- 集成测试
- E2E 测试（Playwright/Cypress）

### 部署

推荐部署平台：
- **Vercel**: 最佳 Next.js 支持
- **Netlify**: 简单易用
- **AWS/Azure**: 企业级部署

---

## 📞 获取帮助

- 查看 `PROJECT_STATUS.md` 了解项目状态
- 查看 `README.md` 了解项目概览
- 检查代码注释了解实现细节

---

**最后更新**: 2024-01-15


